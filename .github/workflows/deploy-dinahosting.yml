# ============================================================
# Build the site and publish it to erasmusinbarcelona.com.
#
# The runner builds dist/ exactly as `npm run build` does, adds the
# production .htaccess generated from src/data/redirects.js, and
# mirrors the result into the hosting account's web root over FTPS.
#
# The transfer is FTPS driven by lftp. The first attempt timed
# out on the control socket, and a probe from a runner explained
# why: port 21 is open and AUTH TLS completes in about a second,
# but the server answers with a certificate for
# *.espacioseguro.com. Dialled by any other name, a client that
# verifies the hostname waits for a handshake that cannot finish.
#
# The fix is to use the name the certificate is actually for.
# With FTP SSL enabled, the panel's FTP section shows it —
# <account>.espacioseguro.com — and FTP_SERVER must be set to
# that, so TLS is verified in full rather than half-trusted. The
# upload refuses to start against any other host, because the
# failure it produces is a thirty-second silence rather than an
# error worth reading.
#
# The mirror deletes files on the server that are no longer in
# dist/, so the live site is always exactly what this repository
# builds. Anything the server keeps for itself is named in the
# exclude list on the upload step.
#
# Credentials live in repository secrets and never appear here.
# SSH and FTP share them on this host, so the FTP_* names still
# fit:
#   FTP_SERVER    the hosting's *.espacioseguro.com FTP host
#   FTP_USERNAME  the account user
#   FTP_PASSWORD  its password
#
# The user lands in the account home, not the web root, so the
# target is /www/ — the directory Apache serves. Mirroring into
# the home itself would delete Maildir, logs and the rest of the
# account, so the path is checked before anything uploads.
# Repository variable DEPLOY_DIR overrides it; SITE_CHECK_URL
# points the post-deploy check somewhere else (the hosting's own
# *.dinaserver.com name, while the domain still answers
# elsewhere).
# ============================================================
name: Deploy to dinahosting

on:
  push:
    branches:
      - main
      - claude/site-health-check-df5ie0
  workflow_dispatch:

# One deployment at a time, and let a running one finish rather
# than leaving the web root half-written.
concurrency:
  group: dinahosting
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://www.erasmusinbarcelona.com/
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # The build needs no dependencies — only Node's own modules.
      # sharp and Playwright are for `npm run images` and `npm run
      # check`, neither of which runs here.
      - name: Build
        run: |
          node build.mjs
          node tools/build-htaccess.mjs

      # The upload deletes whatever is not in dist/, so the one
      # thing that must never be wrong is where it points.
      - name: Refuse to mirror outside the web root
        run: |
          dir='${{ vars.DEPLOY_DIR || '/www/' }}'
          case "$dir" in
            /www/|/www/*) ;;
            *) echo "::error::DEPLOY_DIR is '$dir'. The deploy deletes whatever is not in dist/, so it may only target the web root."; exit 1 ;;
          esac
          echo "web root: $dir"

      # Every guard lives in scripts/guards.mjs, which the shell deploy
      # runs too, so the two paths cannot enforce different things. It
      # refuses a prototype build, a sitemap that disagrees with what was
      # built, an .htaccess that disagrees with redirects.js, a course
      # week that has already happened, and a fact typed into a template
      # instead of read from src/data/.
      - name: Guards
        run: node scripts/guards.mjs

      - name: Publish over FTPS
        env:
          HOST: ${{ secrets.FTP_SERVER }}
          USER: ${{ secrets.FTP_USERNAME }}
          LFTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          DIR: ${{ vars.DEPLOY_DIR || '/www/' }}
        run: |
          case "$HOST" in
            *.espacioseguro.com) ;;
            *)
              echo "::error::FTP_SERVER must be the hosting's <account>.espacioseguro.com name, which the panel shows under FTP once FTP SSL is on. Any other name fails the certificate's hostname check, and fails it by hanging."
              exit 1 ;;
          esac

          sudo apt-get update -qq && sudo apt-get install -y -qq lftp

          # cgi-bin belongs to the server and .well-known is where
          # Let's Encrypt writes its challenge. Neither comes from
          # dist/, so neither may be swept up by --delete.
          # --env-password keeps the password out of the process
          # list, where -u user,pass would put it.
          #
          # Transfers run one at a time. Four at once and the host
          # starts refusing data connections part-way through, killing
          # individual files with "max-retries exceeded" while their
          # neighbours go up fine — a shared server limiting concurrent
          # sessions, not a transport that does not work.
          settings='
            set ftp:ssl-force true
            set ftp:ssl-protect-data true
            set ssl:verify-certificate true
            set ftp:passive-mode true
            # Without -a the server lists no dotfiles, so .htaccess and
            # .nojekyll are invisible to both the mirror and the check
            # below — which then reports as missing two files that are
            # sitting right there.
            set ftp:list-options -a
            set net:max-retries 5
            set net:reconnect-interval-base 5
            set net:timeout 30
          '

          lftp -u "$USER" --env-password "ftp://$HOST" <<LFTP || echo "lftp exited $?"
          $settings
          mirror -R --delete --continue --verbose=1 --exclude-glob .git* --exclude cgi-bin/ --exclude .well-known/ dist/ $DIR
          bye
          LFTP

          # lftp's exit code has proved unreliable here — it reported
          # failure after a run in which every file transferred and no
          # error was printed. So the check is what actually landed:
          # every file in dist/ has to come back in a remote listing.
          # Anything extra on the server is fine (cgi-bin, .well-known);
          # anything missing is not.
          lftp -u "$USER" --env-password "ftp://$HOST" <<LFTP > /tmp/remote-raw.txt
          $settings
          cd $DIR
          find
          bye
          LFTP

          sed 's#^\./##' /tmp/remote-raw.txt | grep -v '/$' | sed '/^$/d' | sort -u > /tmp/remote.txt
          ( cd dist && find . -type f | sed 's#^\./##' ) | sort -u > /tmp/local.txt
          echo "dist/: $(wc -l < /tmp/local.txt) files · server: $(wc -l < /tmp/remote.txt) files"

          missing=$(comm -23 /tmp/local.txt /tmp/remote.txt)
          if [ -n "$missing" ]; then
            echo "::error::These files never made it to the server:"
            echo "$missing"
            exit 1
          fi
          echo "every file in dist/ is on the server"

      # Against the real domain once it points here; against the
      # hosting's own name while it does not.
      #
      # The host rate-limits bursts, and a 429 reads exactly like a
      # broken page. It is what failed every deploy up to run 8: the
      # upload had succeeded, every file was verified on the server, and
      # then the 404 check — the first request here without a --retry —
      # got a 429 and the job went red.
      #
      # Measured against the hosting on 22 August 2026: a burst draws
      # 429 from the fourth request onwards; at three seconds apart four
      # to six get through before one is refused; a 429 clears after
      # about twenty seconds of quiet. Pacing alone is therefore not
      # enough, so every request below goes through one helper that
      # paces itself and also waits a 429 out. --retry is not enough on
      # its own either: it gives up on its own schedule and says nothing
      # about why.
      - name: Check the site answers
        env:
          BASE: ${{ vars.SITE_CHECK_URL || 'https://www.erasmusinbarcelona.com' }}
          PACE: '3'
          BACKOFF: '20'
          RETRIES: '4'
        run: |
          sleep 10
          echo "checking $BASE"

          # Prints the status code, then the Location header. Retries for
          # as long as the host is rate-limiting, then paces itself.
          head_of() {
            local url="$1" attempt=0 headers code
            while :; do
              headers=$(curl -sS -o /dev/null -D - --max-time 30 "$url" 2>/dev/null | tr -d '\r')
              code=$(printf '%s\n' "$headers" | awk '/^HTTP\//{c=$2} END{print c}')
              [ "$code" != "429" ] && break
              attempt=$((attempt + 1))
              [ "$attempt" -gt "$RETRIES" ] && break
              echo "  rate-limited, waiting $((BACKOFF * attempt))s" >&2
              sleep "$((BACKOFF * attempt))"
            done
            sleep "$PACE"
            printf '%s\n' "${code:-000}"
            printf '%s\n' "$headers" | awk 'tolower($1)=="location:"{print $2}'
          }

          for path in / /join-a-course/ /dates/ /contact/ /your-week/; do
            code=$(head_of "$BASE$path" | sed -n 1p)
            echo "$code  $path"
            [ "$code" = "200" ] || exit 1
          done

          code=$(head_of "$BASE/no-such-page/" | sed -n 1p)
          echo "$code  /no-such-page/ (expected 404)"
          [ "$code" = "404" ] || exit 1

          for legacy in /about-us/:/about/ /program-information/:/your-week/ /we-come-to-you/:/plan-a-mobility/; do
            from=${legacy%%:*}; to=${legacy##*:}
            location=$(head_of "$BASE$from" | sed -n 2p)
            echo "$from -> $location (expected $to)"
            case "$location" in *"$to") ;; *) exit 1 ;; esac
          done
